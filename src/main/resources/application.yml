# # local 환경
# server:
#     port: 8888

# spring:
#     datasource:
#         driver-class-name:  org.mariadb.jdbc.Driver
#         url:                jdbc:mariadb://localhost:3306/inspire   # 마지막은 DB 이름
#         username:           inspire
#         password:           '$pp020719'
    
#     jpa:
#         hibernate: 
#             # create, create-drop, update, none
#             ddl-auto:  create
#             # 콘솔출력
#         show-sql: true     
#         properties:
#             hibernate:
#                 # h2
#                 # dialect: org.hibernate.dialect.H2Dialect
#                 # mariadb
#                 dialect: org.hibernate.dialect.MariaDBDialect
#                 format_sql: true
#     ai:
#         openai:
#             model: ${OPEN_AI_MODEL}
#             api:
#                 key: ${OPEN_AI_KEY}
#                 url: ${OPEN_AI_URL}
#     data:
#         redis:
#             host: localhost
#             port: 6380
#             password: 
#             timeout: 6000ms
# jwt:
#     secret: jwt-secret-key-gh719719-1234567890
#         # # redis
#         # 도커, 이미지 생성 # docker run -d --name redis-container -p 6379:6379 redis:7.2
#         # 도커 접속? # docker exec -it redis-container redis-cli
#         # docker run -d --name redis-insight -p 8001:8001 redis/redisinsight:latest
#         # Host: redis 또는 host.docker.internal , port: 6379
    

# openApi:
#     serviceKey: ${SERVICE_KEY}
#     callbackUrl: ${CALLBACKURL}
#     dataType: ${TYPE}
    

# # # mybatis settings
# # mybatis:
# #     mapper-locations: /mappers/**/*Mapper.xml
# #     config-location: classpath:/mappers/config/mybatis-config.xml

# cloud 환경
server:
    port: 8888

spring:

    # mariadb settings
    datasource:
        driver-class-name:  org.mariadb.jdbc.Driver
        url:                ${SPRING_DATASOURCE_URL:jdbc:mariadb://localhost:3306/inspire?useSSL=true&requireSSL=true}  
        username:           ${SPRING_DATASOURCE_USERNAME:root}
        password:           ${SPRING_DATASOURCE_PASSWORD:$pp020719}
    
    jpa:
        hibernate: 
            # create, create-drop, update, none
            ddl-auto:  ${SPRING_JPA_HIBERNATE_DDL_AUTO:create}
            # 콘솔출력
        show-sql: ${SPRING_JPA_SHOW_SQL:true}
        properties:
            hibernate:
                # h2
                # dialect: org.hibernate.dialect.H2Dialect
                # mariadb
                dialect: org.hibernate.dialect.MariaDBDialect
                format_sql: true
    
    # # redis
    # 도커, 이미지 생성 # docker run -d --name redis-container -p 6379:6379 redis:7.2
    # 도커 접속? # docker exec -it redis-container redis-cli
    # docker run -d --name redis-insight -p 8001:8001 redis/redisinsight:latest
    # Host: redis 또는 host.docker.internal , port: 6379

    data:
        redis:
            host: ${SPRING_REDIS_HOST:redis}
            port: ${SPRING_REDIS_PORT:6382}
            password: 
            timeout: 6000ms
jwt:
    secret: jwt-secret-key-gh719719-1234567890



